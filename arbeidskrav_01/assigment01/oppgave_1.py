#

total_lessons = int(input('Tap in total lessons: '))
minutes_per_lesson = int(input('Duration per lesson is: '))

total_time =  int (total_lessons * minutes_per_lesson)

hours = int(total_time // 60)
rest_minutes = int(total_time % 60)

while rest_minutes or hours is not =< 0,('')
if rest_minutes and hours > 0:
        print(f'{total_time} minutes are {hours} hours and {rest_minutes} minutes')
   elif rest_minutes and hours

    else:
        print(f'Try agaim ')

print(f'{total_time}')

